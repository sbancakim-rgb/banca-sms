# 방카 문자 생성기

사진을 올리면 은행 팀장님께 보낼 문자 초안을 톤이 다른 3가지로 만들어 줍니다.

**앱 주소** — https://sbancakim-rgb.github.io/banca-sms/

## 두 가지 모드

### 글귀 · 명언 (기본)
좋은 글귀나 명언 이미지를 올리면, **그 글귀와 함께 보낼 짧은 문자**를 만듭니다.
글귀를 요약하거나 되풀이하지 않고, 거기서 한 번 더 생각해볼 지점을 짚어 안부로 마무리합니다.

톤 12종 중에서 3개를 고릅니다 — 담백한 사색형 · 일상 연결형 · 따뜻한 안부형 · 짧고 단단한 한 줄 · 질문을 남기는 형 · 계절·날씨 연결형 · 감사 전하는 형 · 경험 고백형 · 담백한 응원형 · 가벼운 미소형 · 한 주 여는 형 · 한 주 닫는 형

### 상품 · 시책
시책표 · 안내문 · 실적표를 읽고 안내 문자를 만듭니다.
자료에 적힌 숫자만 쓰고, `(광고)` 표기와 무료수신거부 문구를 넣는 스위치가 있습니다.

톤 8종 중에서 3개를 고릅니다 — 정중한 공지형 · 친근한 안부형 · 실적 독려형 · 핵심만 요약형 · 마감 임박형 · 감사 겸 안내형 · 질문 받는 형 · 현장 도움형

맨 아래 `직접 입력`을 고르면 원하는 톤을 직접 적을 수 있습니다.
마음에 드는 조합은 `지금 조합을 기본으로 저장`을 눌러 기본으로 삼으세요.
모드마다 톤 · 길이 · 설정이 **따로** 저장됩니다.

### 포인트 구절 고르기 (글귀 모드)
사진을 올리고 `글귀 읽고 포인트 고르기`를 누르면 글귀를 원문 그대로 읽어 주고, 그중 중심으로 삼을 만한 구절을 나눠 줍니다. 원하는 구절을 누르면 **세 문자 모두 그 구절에서 출발**합니다. 안 고르고 바로 만들어도 됩니다.

## 내 문체 — 이게 핵심입니다

지금까지 보내신 문자를 넣어두면 그 **말투 · 문장 길이 · 호칭 · 맺음말**을 따라 씁니다.
내용은 베끼지 않고 문체만 가져옵니다. **3~10개 정도면 충분히 달라집니다.**

넣는 방법 두 가지:
- **붙여넣기** — 보낸 문자를 복사해서 넣고 `붙여넣은 것 추가`. 여러 개면 사이에 빈 줄을 넣으세요.
- **캡처에서 가져오기** — 문자 앱 캡처를 올리면 본인이 보낸 본문만 골라 옮겨 적습니다 (한 번에 5장까지)

목록의 글을 **눌러서 바로 고칠 수 있고**, 고치면 자동으로 저장됩니다. 지울 때는 옆의 `×`를 누르세요.

## 지난 기록 (라이브러리)

만든 문자는 **사진까지 통째로 자동 저장**됩니다. 날아가지 않습니다.

`지난 기록`을 펼치면 날짜 · 사진 · 비용과 함께 목록이 나옵니다.
- `펼쳐 보기` — 그때 읽은 글귀, 고른 포인트, 문자 3개를 모두 봅니다. **그 자리에서 고치면 기록에도 저장**됩니다.
- `복사` — 바로 복사해서 보냅니다. 돈 들여 다시 만들 필요가 없습니다.
- `이 사진으로 다시 만들기` — 그때 사진 · 글귀 · 포인트 · 길이를 작업칸에 그대로 되돌려 놓습니다. 톤만 바꿔서 다시 뽑을 때 좋습니다.
- 위쪽 칸에 단어를 넣으면 글귀나 문자 내용으로 찾습니다.
- 최근 **80건**까지 보관하고, 넘으면 오래된 것부터 지웁니다. `×`로 하나씩, `기록 전체 지우기`로 모두 지울 수 있습니다.

기록은 그 기기 안에만 저장됩니다(사진 때문에 용량이 커서 백업 글자에는 들어가지 않습니다).

## 사진 크게 보기

올린 사진이나 기록 속 사진을 **누르면 전체화면으로 크게** 볼 수 있습니다. 화면을 다시 누르거나 오른쪽 위 `×`로 닫습니다.

## 처음 한 번만

### 홈 화면에 추가
- **아이폰(Safari)**: 아래쪽 공유 버튼 → `홈 화면에 추가`
- **안드로이드(Chrome)**: 메뉴 `⋮` → `앱 설치` 또는 `홈 화면에 추가`

그 뒤로는 아이콘만 누르면 주소창 없이 전체화면으로 열립니다.

### API 키
1. https://console.anthropic.com → 로그인 → **API Keys** → **Create Key**
2. `sk-ant-...` 키를 복사 (창을 닫으면 다시 볼 수 없습니다)
3. **Billing** 에서 크레딧 충전 (최소 $5)
4. 앱의 `연결 설정` 에 붙여넣기

키는 그 기기에만 저장됩니다. 소스 코드에도 저장소에도 들어가지 않습니다.

## 쓰는 순서
1. 모드 고르기
2. `촬영하기` 또는 `사진 고르기` (최대 5장)
3. (글귀 모드) `글귀 읽고 포인트 고르기` → 잡고 싶은 구절 누르기
4. 길이와 톤 3가지 확인
5. 특별히 넣을 말이 있으면 `덧붙일 내용` 에 (예: "월요일 아침에 보낼 것")
6. 아래 `문자 3개 만들기`
7. 마음에 드는 걸 **직접 고친 뒤** `복사`

## 알아두면 좋은 것
- 결과 맨 위에 **사진에서 읽은 내용**이 뜹니다. 글귀를 잘못 읽었는지 여기서 확인하세요.
- **글자 수 / byte**: 90byte 이하면 SMS 단문, 넘으면 LMS 장문. 한글은 1자당 2byte.
- 사진은 보내기 전 자동으로 축소되니 폰으로 찍은 큰 사진도 그냥 올리시면 됩니다.
- 인터넷 없이도 앱 화면은 열립니다. 생성만 인터넷이 필요합니다.
- 결과가 마음에 안 들면 `다시 3개 뽑기` — 누를 때마다 새로 씁니다.
- **문자에 서명은 들어가지 않습니다.** 본문만 만듭니다.

## 비용

생성할 때마다 결과 아래에 **실제로 든 금액**이 표시됩니다. Sonnet 5 기준 대략:

| 문자 길이 | 대략 |
|---|---|
| 200자 × 3개 | 약 40원 |
| 600자 × 3개 | 약 70원 |
| 1000자 × 3개 | 약 95원 |
| 글귀 읽기 | 약 12원 |

길이를 5배로 늘려도 비용은 2배가 조금 넘습니다. 사진과 프롬프트가 차지하는 입력 비용은 길이와 상관없이 고정이라서요.
모델을 Opus 5로 바꾸면 약 2.5배, Haiku 4.5로 바꾸면 약 절반입니다.
**이미 만든 문자는 `지난 기록`에서 다시 꺼내 쓰세요. 돈이 들지 않습니다.**

## 지켜 주세요
- 글귀의 지은이나 출처가 사진과 다르게 적혔으면 고쳐서 보내세요.
- 상품 문자는 상품명 · 수수료율 · 기간 · 날짜를 **원본과 대조**한 뒤 발송하시고, 소속사 **준법감시(광고심의)** 기준을 따르세요.
- 고객 실명 · 주민번호 · 계좌번호가 찍힌 사진은 올리지 마세요.

## 잘 안 될 때
| 증상 | 해결 |
|---|---|
| "API 키가 올바르지 않습니다" | 키 앞뒤 공백 확인, 콘솔에서 새 키 발급 |
| "계정 잔액이 부족합니다" | 콘솔 Billing 에서 크레딧 충전 |
| "Claude가 문자를 만들지 못했습니다" | 오류 상자의 `Claude가 실제로 답한 내용`을 펼쳐보면 이유가 나옵니다 |
| "연결하지 못했습니다" | 인터넷 확인. 회사 와이파이가 막으면 데이터로 시도 |
| 고친 내용이 반영 안 됨 | 앱을 완전히 닫았다 다시 열기 (캐시 갱신) |

## 파일 구성
`index.html` 앱 본체 · `manifest.webmanifest` 설치 정보 · `sw.js` 오프라인 캐시 · `icons/` 홈 화면 아이콘
