# 방카 문자 생성기

시책표 · 안내문 · 실적표 사진을 올리면, 은행 팀장님께 보낼 문자 초안을 톤이 다른 3가지로 만들어 줍니다.

## 처음 한 번만 — 설치와 키

### 1. 홈 화면에 추가 (앱처럼 쓰기)
앱 주소를 폰 브라우저에서 연 다음:

- **아이폰(Safari)**: 아래쪽 공유 버튼 → `홈 화면에 추가`
- **안드로이드(Chrome)**: 오른쪽 위 메뉴 `⋮` → `앱 설치` 또는 `홈 화면에 추가`

그 뒤로는 아이콘만 누르면 **주소창 없이 전체화면**으로 열립니다.
앱 안에서도 오른쪽 위 `⤓` 버튼으로 설치할 수 있습니다.

### 2. API 키 넣기
1. https://console.anthropic.com 접속 → 로그인
2. 왼쪽 메뉴 **API Keys** → **Create Key** → 생성
3. `sk-ant-...` 로 시작하는 키를 복사 (창을 닫으면 다시 볼 수 없으니 바로 복사)
4. **Billing** 에서 크레딧 충전 (최소 $5, 문자 1건 생성에 대략 5~20원)
5. 앱의 `연결 설정` → `Claude API 키` 칸에 붙여넣기

키는 **그 기기의 브라우저에만** 저장됩니다. 소스 코드에도, 저장소에도 들어가지 않습니다.
기기를 바꾸거나 앱을 지웠다 다시 깔면 키를 다시 넣어야 합니다.

## 쓰는 순서
1. `촬영하기` 로 시책표를 바로 찍거나, `사진 고르기` 로 앨범·캡처에서 선택 (최대 5장)
2. 보내는 사람 서명 · 받는 분 · 문자 길이 선택 — 한 번 넣으면 계속 유지됩니다
3. 강조할 내용이 있으면 적기 (예: "금요일 마감 강조", "상품명은 정식 명칭 그대로")
4. 아래 `문자 3개 만들기` 누르기
5. 결과 카드에서 문장을 **직접 고친 뒤** `복사`

## 알아두면 좋은 것
- **사진에서 읽은 내용**이 결과 맨 위에 요약으로 뜹니다. 숫자를 잘못 읽었는지 여기서 바로 확인하세요.
- **글자 수 / byte**: 90byte 이하면 SMS 단문, 넘으면 LMS 장문입니다. 한글은 1자당 2byte.
- **톤 바꾸기**: 3가지 톤의 이름과 설명을 원하는 대로 고칠 수 있습니다.
- **광고성 문자**: 켜면 맨 앞 `(광고)` 표기와 무료수신거부 문구가 자동으로 붙습니다.
- **모델**: Sonnet 5 가 기본입니다. 표가 복잡하거나 결과가 아쉬우면 Opus 5 로 바꿔 보세요.
- 사진은 보내기 전에 자동으로 축소됩니다. 폰으로 찍은 큰 사진도 그냥 올리시면 됩니다.
- 인터넷이 없어도 앱 화면은 열립니다. 다만 문구 생성은 인터넷이 필요합니다.

## 반드시 지켜 주세요
- 생성된 문구는 **초안**입니다. 상품명 · 수수료율 · 기간 · 날짜는 **원본 자료와 대조**한 뒤 발송하세요.
- 보험 · 금융상품 안내 문자는 소속사 **준법감시(광고심의)** 기준을 따라야 합니다.
- 고객 실명 · 주민번호 · 계좌번호가 찍힌 사진은 올리지 마세요.

## 잘 안 될 때
| 증상 | 해결 |
|---|---|
| "API 키가 올바르지 않습니다" | 키 앞뒤 공백 확인, 콘솔에서 새 키 발급 |
| "계정 잔액이 부족합니다" | 콘솔 Billing 에서 크레딧 충전 |
| "연결하지 못했습니다" | 인터넷 확인. 회사 와이파이/보안 프로그램이 막을 수 있으니 데이터로 시도 |
| 숫자가 자료와 다름 | 사진을 더 밝고 선명하게, 또는 정확한 숫자를 `강조할 내용`에 직접 적기 |
| 고친 내용이 반영 안 됨 | 앱을 완전히 닫았다 다시 열기 (캐시 갱신) |

## 파일 구성
`index.html` 앱 본체 · `manifest.webmanifest` 설치 정보 · `sw.js` 오프라인 캐시 · `icons/` 홈 화면 아이콘
